#include "Terrain.h"

void Terrain::GenerateVertices(std::vector<float> noiseMap)
{
    std::vector<float> vertices;

    int rez = 20;
    for (int i = 0; i <= rez - 1; i++)
    {
        for (int j = 0; j <= rez - 1; j++)
        {

        }
    }
}